<%args>
  $objects
</%args>

<& /page/columnar_table.mas,
  headings => [ "類型(Type)", "名稱(Name)" ],
  data     => SGN::View::Stock::stock_table( $objects ),
&>

<%once>
    use SGN::View::Stock ();
</%once>

