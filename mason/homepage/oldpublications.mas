
<%perl>
use strict;
use File::Spec;
use CXGN::Tools::File;
use CXGN::Page;

my $file=File::Spec->catfile($c->get_conf('homepage_files_dir'), 'publications.txt');
my $page=CXGN::Page->new('Publications','john');


my $publications = qq { <p class="boxbgcolor5"><br />由於伺服器問題，出版物目前無法取得。造成不便敬請見諒，請稍後再試(Publications are not available at the moment due to a server problem)。<br /><br /></p><br /><br /><br /><br /> };

eval { 

  $publications = $m->file($file);

};


</%perl>

<& /page/page_title.mas, title=>"所有出版物(All Publications)" &>
<% $publications %>

