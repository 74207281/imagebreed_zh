<%args>
  $default_ref_base => ''
</%args>
% my $items = Load( $m->content );
% delete $ARGS{$_} for qw( default_ref_base c );

<&| /page/info_section.mas, title => '資料集(Data Sets)', %ARGS &>
<ul>

% foreach my $item (@$items) {
  <li>
%    if( my $r = $item->{ref} ) {
%       unless( $r =~ m!^/! || $r =~ m!^(ht|f)tp://! ) { $r = "$default_ref_base/$r" }
        <a href="<% $r %>"><% $item->{text} %></a>
%    } else {
        <span class="ghosted"><% $item->{text} %></span>
          <div style="margin-left: 2em; font-size: 90%">（檔案暫時無法使用，詳情請聯繫 <a href="mailto:jr286@cornell.edu">Joss Rose</a>）((file temporarily unavailable, contact Joss Rose for details))</div>

%    }
  </li>
% }
</ul>
</&>

<%once>
  use YAML::Any;
</%once>
